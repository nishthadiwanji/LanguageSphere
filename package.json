{
  "name": "languagesphere-monorepo",
  "version": "1.0.0",
  "description": "LanguageSphere monorepo for Vercel deployment",
  "scripts": {
    "build": "cd languagesphere && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "mongoose": "^7.6.3",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "express-validator": "^7.0.1"
  }
}

